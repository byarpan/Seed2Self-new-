// TODO: Auth request payload validators
export const validateRegister = (data) => {
  return { isValid: true, errors: [] };
};

export const validateLogin = (data) => {
  return { isValid: true, errors: [] };
};
